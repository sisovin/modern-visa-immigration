{
  "name": "frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd ../backend && python manage.py runserver\" \"next dev\"",
    "build": "next build",
    "start": "concurrently \"cd ../backend && python manage.py runserver\" \"next start\""
  },
  "devDependencies": {
    "concurrently": "^6.2.1"
  }
}
